name: emails
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - google-api-python-client
  - google-auth-httplib2
  - google-auth-oauthlib
  - python-dotenv
  - requests
  - pip:
    - # If you have local custom modules that need pip installs, list them here
    - .
